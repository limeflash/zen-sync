# SQLite database directory
ZENSYNC_DATA_DIR=./data

# Registration gate (fail-closed). Set a token to require it on /api/register,
# OR allow open signup. If neither is set, all registration is rejected.
ZENSYNC_REG_TOKEN=
ZENSYNC_ALLOW_OPEN_REGISTRATION=false
